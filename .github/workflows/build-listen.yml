name: Build GeoTrinity

on:
  push:
    branches: [build]
  workflow_dispatch:

jobs:
  build:
    name: Build Auto (Game target)
    runs-on: self-hosted
    timeout-minutes: 480

    steps:
      - name: Notify start
        shell: powershell -ExecutionPolicy Bypass -File "{0}"
        run: |
          $time = Get-Date -Format "HH:mm:ss"
          $hash = & "C:\Program Files\Git\cmd\git.exe" -C "H:\Work\Git\GeoTrinity" log -1 --format="%h" 2>$null
          msg * /TIME:120 "GeoTrinity Build Starting! $time | build-Auto @ $hash"

      - name: Sync build
        shell: powershell -ExecutionPolicy Bypass -File "{0}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $token = [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("x-access-token:$env:GITHUB_TOKEN"))
          $env:GIT_TERMINAL_PROMPT = "0"
          $git = "C:\Program Files\Git\cmd\git.exe"
          & $git -C "H:\Work\Git\GeoTrinity" -c "credential.helper=" -c "http.https://github.com/.extraheader=Authorization: Basic $token" fetch origin build-listen
          & $git -C "H:\Work\Git\GeoTrinity" checkout build
          & $git -C "H:\Work\Git\GeoTrinity" reset --hard FETCH_HEAD

      - name: Package Game
        shell: powershell -ExecutionPolicy Bypass -File "{0}"
        run: |
          $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
          $archiveDir = "H:\Work\Git\GeoTrinity\Build\GeoTrinity_$timestamp"
          & "H:\Epic\UE_5.7\Engine\Build\BatchFiles\RunUAT.bat" BuildCookRun `
            -project="H:\Work\Git\GeoTrinity\GeoTrinity.uproject" `
            -noP4 -platform=Win64 -clientconfig=Development `
            -cook -build -stage -pak -archive `
            -archivedirectory="$archiveDir"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          Move-Item "$archiveDir\Windows\*" "$archiveDir\\"
          Remove-Item "$archiveDir\Windows" -ErrorAction SilentlyContinue
          msg * /TIME:120 "GeoTrinity Build Done! Output: $archiveDir"
        env:
          UE_IgnoreOutdatedImportedLibraries: 1
