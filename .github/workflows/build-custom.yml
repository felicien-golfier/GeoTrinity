name: Build GeoTrinity (Custom)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: Branch to build
        required: true
        default: master
      config:
        description: Build configuration
        required: true
        default: Development
        type: choice
        options:
          - Development
          - DebugGame
          - Test
          - Shipping
      build_server:
        description: Also build dedicated server
        required: false
        default: false
        type: boolean
      clean:
        description: Clean build (wipe Binaries, Intermediate and cook cache)
        required: false
        default: false
        type: boolean

jobs:
  build:
    name: Build ${{ github.event.inputs.branch }} (${{ github.event.inputs.config }})
    runs-on: self-hosted
    timeout-minutes: 480

    steps:
      - name: Notify start
        shell: powershell -ExecutionPolicy Bypass -File "{0}"
        run: |
          $time = Get-Date -Format "HH:mm:ss"
          $server = if ("${{ github.event.inputs.build_server }}" -eq "true") { " + Server" } else { "" }
          msg * /TIME:120 "GeoTrinity Build Starting! $time | ${{ github.event.inputs.branch }} (${{ github.event.inputs.config }}$server)"

      - name: Sync branch
        shell: powershell -ExecutionPolicy Bypass -File "{0}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $token = [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("x-access-token:$env:GITHUB_TOKEN"))
          $env:GIT_TERMINAL_PROMPT = "0"
          $git = "C:\Program Files\Git\cmd\git.exe"
          $branch = "${{ github.event.inputs.branch }}"
          & $git -C "H:\Work\Git\GeoTrinity" -c "credential.helper=" -c "http.https://github.com/.extraheader=Authorization: Basic $token" fetch origin $branch
          & $git -C "H:\Work\Git\GeoTrinity" checkout $branch
          & $git -C "H:\Work\Git\GeoTrinity" reset --hard FETCH_HEAD

      - name: Clean (optional)
        if: ${{ github.event.inputs.clean == 'true' }}
        shell: powershell -ExecutionPolicy Bypass -File "{0}"
        run: |
          & "H:\Work\Git\GeoTrinity\Clean.ps1"

      - name: Package Game
        shell: powershell -ExecutionPolicy Bypass -File "{0}"
        run: |
          $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
          $branch = "${{ github.event.inputs.branch }}" -replace "[/\\]", "-"
          $config = "${{ github.event.inputs.config }}"
          $withServer = "${{ github.event.inputs.build_server }}" -eq "true"
          $archiveDir = "H:\GeoTrinity\Builds\GeoTrinity_${branch}_${config}_$timestamp"

          $serverArgs = if ($withServer) { @("-server", "-serverconfig=$config") } else { @() }

          & "H:\Epic\UE_5.7\Engine\Build\BatchFiles\RunUAT.bat" BuildCookRun `
            -project="H:\Work\Git\GeoTrinity\GeoTrinity.uproject" `
            -noP4 -platform=Win64 -clientconfig=$config `
            -cook -build -stage -pak -archive `
            -archivedirectory="$archiveDir" `
            @serverArgs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

          Move-Item "$archiveDir\Windows\*" "$archiveDir\\"
          Remove-Item "$archiveDir\Windows" -ErrorAction SilentlyContinue
          if ($withServer) {
            Rename-Item "$archiveDir\WindowsServer" "Server"
          }

          msg * /TIME:120 "GeoTrinity Build Done! Output: $archiveDir"
        env:
          UE_IgnoreOutdatedImportedLibraries: 1
