name: Build GeoTrinity Auto

on:
  push:
    branches: [build]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-custom.yml
    with:
      branch: build
      config: Development
      build_server: false
      clean: false
    secrets: inherit
